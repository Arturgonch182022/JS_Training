//3. Подсчёт длины строк
// Создайте стрелочную функцию,
// которая принимает массив строк и возвращает массив их длин.
const arrayStrings = (s) => s.map((lenth)=> lenth.length);
console.log(arrayStrings(["artur", "top"]));